spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipes_dev
    username: recipes_dev_user
    password: password1
    platform: mysql
  #    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: src/main/scripts/recipes_database_create.sql